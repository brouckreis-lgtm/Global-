"use client";
import { useState } from 'react';

export default function TransferCalc() {
  const [amount, setAmount] = useState(0);
  const [rate, setRate] = useState(2); // Ton taux de 2% (modifiable via Admin)

  const fee = (amount * rate) / 100;

  return (
    <div className="glass-panel p-8 max-w-md mx-auto border-gold/30">
      <h3 className="text-2xl font-bold mb-6 gold-gradient">Corridor Financier</h3>
      
      <input 
        type="number" 
        onChange={(e) => setAmount(Number(e.target.value))}
        placeholder="Montant à envoyer"
        className="w-full bg-black/40 border border-white/10 p-4 rounded-xl mb-4 focus:border-gold outline-none"
      />

      <div className="space-y-3 mb-8 text-sm">
        <div className="flex justify-between">
          <span className="text-gray-500">Taux Global Study</span>
          <span className="text-gold">{rate}%</span>
        </div>
        <div className="flex justify-between font-bold text-lg">
          <span>Frais totaux</span>
          <span>{fee.toLocaleString()} UNITÉS</span>
        </div>
      </div>

      <div className="space-y-4">
        <button className="w-full py-4 bg-gold text-black font-black rounded-xl uppercase">
          Via Fedapay (Mobile Money)
        </button>
        <div className="p-4 border border-dashed border-gold/40 rounded-xl text-center">
          <p className="text-[10px] uppercase mb-2">Adresse USDT BEP20</p>
          <code className="text-[10px] block break-all text-gray-400">0x395df0b6add75e86074cd8032a1d6ff293309e20</code>
          <p className="text-gold text-[10px] mt-2">Envoyez le Hash sur WhatsApp après transfert</p>
        </div>
      </div>
    </div>
  );
}
